export interface BuildingConfig {
  id: 'about' | 'skills' | 'projects' | 'journey' | 'contact';
  x: number;
  y: number;
  w: number;
  img: string;
  alt: string;
  label: string;
  labelColor: string;
}
